name: Deploy API

on:
  push:
    branches: [main]
    paths:
      - 'packages/api/**'
      - '.github/workflows/deploy-api.yml'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deploy environment'
        required: true
        default: 'production'
        type: choice
        options:
          - staging
          - production

# Minimum-privilege default. The Cloudflare deploy doesn't push commits
# or create issues; checkout + read of the workflow definition is all
# the GITHUB_TOKEN side needs. Wrangler authenticates via the
# CLOUDFLARE_API_TOKEN secret separately.
permissions:
  contents: read

jobs:
  test:
    name: Test API
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: pnpm/action-setup@v5

      - uses: actions/setup-node@v7.0.0
        with:
          node-version: '22'
          cache: 'pnpm'

      # Install only the API workspace and its transitive deps. The full
      # `pnpm install` would also run apps/desktop's postinstall (electron-builder
      # install-app-deps -> better-sqlite3 native rebuild), which fails on the
      # Linux + Node 22 runner against current Electron headers. The API worker
      # has no need for any of that.
      - run: pnpm install --filter '@dripnex/api...' --ignore-scripts

      - name: Typecheck
        run: pnpm --filter @dripnex/api typecheck

      - name: Test
        run: pnpm --filter @dripnex/api test

  deploy:
    name: Deploy API
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: pnpm/action-setup@v5

      - uses: actions/setup-node@v7.0.0
        with:
          node-version: '22'
          cache: 'pnpm'

      - run: pnpm install --filter '@dripnex/api...' --ignore-scripts

      - name: Embed migrations
        run: pnpm --filter @dripnex/api db:embed

      - name: Determine environment
        id: env
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "target=${{ inputs.environment }}" >> "$GITHUB_OUTPUT"
          else
            echo "target=production" >> "$GITHUB_OUTPUT"
          fi

      - name: Migrate Turso
        env:
          TURSO_DATABASE_URL: ${{ secrets.TURSO_DATABASE_URL }}
          TURSO_AUTH_TOKEN: ${{ secrets.TURSO_AUTH_TOKEN }}
        run: |
          if [ -z "${TURSO_DATABASE_URL}" ]; then
            echo "TURSO_DATABASE_URL unset — skip migrate"
            exit 0
          fi
          pnpm --filter @dripnex/api db:migrate

      - name: Deploy to Cloudflare Workers
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          DEPLOY_ENV: ${{ steps.env.outputs.target }}
        run: npx wrangler deploy --env "$DEPLOY_ENV"
        working-directory: packages/api

      - name: Verify API
        if: steps.env.outputs.target == 'production'
        run: |
          echo "workers.dev:"
          curl -fsS https://readied-api-production.readied.workers.dev/health || echo "workers.dev not ready"
          echo
          echo "api.dripnex.app:"
          curl -fsS --retry 4 --retry-delay 3 --retry-all-errors https://api.dripnex.app/health \
            || echo "custom domain not resolving here yet — workers.dev is the fallback"
