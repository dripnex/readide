name: PR title

# Runs commitlint against the PR title as a standalone status check.
#
# Why standalone (and not a step inside ci.yml `lint`):
# - Feature PRs squash-merge: the PR title becomes the commit on develop.
# - Promotion PRs (develop → main) use a merge commit so feat history lives.
# - semantic-release reads conventional types. A title like
#   "release: audit..." (PR #245) is why v0.15.0 silently cut nothing.
# - Pulling the check into its own workflow gives it a clear, stable
#   status-check name so branch protection on develop and main can require
#   it independently of the broader CI lint job.

on:
  pull_request:
    types: [opened, edited, reopened, synchronize]

permissions:
  contents: read

concurrency:
  # Re-running on title edits is cheap; cancel any in-flight check for
  # the same PR when a new event arrives.
  group: pr-title-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  commitlint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - uses: pnpm/action-setup@v5

      - uses: actions/setup-node@v7.0.0
        with:
          node-version: '22'
          cache: 'pnpm'

      # Dependabot lockfiles resolve @electron/node-gyp as git@github.com:.
      # Without this rewrite, `pnpm install` dies on SSH (see #544 / e2e).
      - name: Force HTTPS for GitHub git dependencies
        run: git config --global 'url.https://github.com/.insteadOf' 'git@github.com:'

      # Title check only needs commitlint + its config — no workspace
      # native deps, no postinstall.
      - name: Install commitlint
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Lint PR title
        env:
          PR_TITLE: ${{ github.event.pull_request.title }}
        run: |
          printf '%s' "$PR_TITLE" | pnpm commitlint
